name: strata_core
description: Core domain entities, failures, logger contracts, and storage interfaces for Strata framework.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  equatable: ^2.0.7
  fpdart: ^1.1.0
  get_it: ^9.0.5
  intl: ^0.20.2
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
